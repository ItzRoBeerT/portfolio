---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/common/Header.astro';
import { getLangFromUrl, useTranslations } from '@/utils/i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);


const seoProps = {
	title: 'Roberto - Desarrollador Full-Stack',
	description:
		'Desarrollador Full-Stack especializado en React, Astro y tecnologías web modernas. Construyendo experiencias web rápidas, accesibles y hermosas.',
};
---

<BaseLayout {...seoProps}>
	<Header />

	<main class="min-h-screen flex items-center justify-center bg-gray-50 pt-20">
		<div class="text-center">
			<h1 class="text-6xl font-bold mb-4">{t('hero.greeting')} Roberto! 🎨</h1>
			<p class="text-xl text-gray-600">{t('hero.role')}</p>
			<p class="mt-4 text-gray-500 max-w-md mx-auto">{t('hero.description')}</p>

			<div class="mt-8 flex gap-4 justify-center">
				<button class="px-6 py-3 bg-primary-500 text-white rounded-lg hover:bg-primary-600 transition-colors">
					{t('hero.cta.primary')}
				</button>
				<button
					class="px-6 py-3 border border-gray-300 text-gray-700 rounded-lg hover:border-primary-500 hover:text-primary-500 transition-colors"
				>
					{t('hero.cta.secondary')}
				</button>
			</div>
		</div>
	</main>
</BaseLayout>
